name: Run shell Commands
on: [push]

jobs:
   run-shell-commands:
    run-on: ubuntu-latest
    steps:
     - name: Echo a String
       run: echo "Hello Security Gurus!! This is our first GitHub Action Workflow file"
